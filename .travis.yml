sudo: require

language: cpp

os:
    - linux

compiler:
    - gcc
    - clang

dist: trusty

source:
    - CI/cmake_install.sh
    - CI/gcc_install.sh
    - CI/gtest_install.sh

script:
    - cmake CMakeLists.txt
    - make
    - ./RefactoringKata
