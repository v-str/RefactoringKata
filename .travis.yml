sudo: require

language: cpp

os:
    - linux

compiler:
    - gcc
    - clang

dist: trusty

before_install:
    - chmod 755 travis/install_dependencies.sh

install:
    - ./travis/install_dependencies.sh

script:
    - cmake CMakeLists.txt
    - make
    - ./RefactoringKata
